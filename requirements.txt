# ---------------------------------------------------------------------------
# Core pipeline. Python 3.10 - 3.12 (3.11 recommended).
#
# INSTALL PyTorch FIRST, separately, with the right CUDA build:
#   NVIDIA GPU (RTX 4050, CUDA 12.x):
#     pip install torch --index-url https://download.pytorch.org/whl/cu121
#   CPU only:
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then:  pip install -r requirements.txt
# ---------------------------------------------------------------------------

# --- tokenizer + data (no GPU needed) --------------------------------------
numpy>=1.26,<3
pandas>=2.1
PyYAML>=6.0
regex>=2023.12
tqdm>=4.66

# --- statistics (thesis: "SciPy") ------------------------------------------
scipy>=1.11

# --- MT evaluation ---------------------------------------------------------
sacrebleu>=2.4.0          # BLEU (eq. 8) and chrF++ (eq. 9)

# --- NLLB-200 backbone -----------------------------------------------------
transformers>=4.41,<5
sentencepiece>=0.2.0      # required by NllbTokenizer
tokenizers>=0.19
safetensors>=0.4
accelerate>=0.30
huggingface-hub>=0.23
datasets>=2.19            # optional, only if you switch to HF Datasets loading

# --- optional but recommended ---------------------------------------------
matplotlib>=3.8           # learning curves, fertility histograms for Chapter 4
